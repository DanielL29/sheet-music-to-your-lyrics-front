import styled from 'styled-components';

const Container = styled.div`
  margin: 0 auto;
  margin-bottom: 100px;
  width: 1200px;

  form {
    display: flex;
    flex-direction: column;
  }
`;

const AddMusicWrapper = {
  Container,
};

export default AddMusicWrapper;
